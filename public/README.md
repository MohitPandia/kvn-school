# Public assets

- **`/logo.svg`** — School logo. Replace this file with your own logo (or use `logo.png` and update the Navbar/layout to reference `/logo.png`).
- **`/images/carousel/`** — Photos for the landing-page carousel. Add images here and list them in `src/lib/gallery.ts`.
